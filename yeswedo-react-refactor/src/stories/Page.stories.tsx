import React from 'react'
import { Meta } from '@storybook/react'
import { HomePage } from '../pages/HomePage/HomePage'

export default {
    title: 'Pages',
} as Meta

export const Home = (
    <HomePage />
)